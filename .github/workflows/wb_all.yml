name: WB → Google Sheets (все скрипты)
 
on:
  workflow_dispatch:
    inputs:
      script:
        description: "Какой скрипт запустить"
        required: true
        default: "все"
        type: choice
        options:
          - все
          - заказы_месяц
          - заказы_неделя
          - реклама
          - реклама_фин
          - фин_отчеты
 
  schedule:
    # Таблица 1: 1-го числа каждого месяца в 08:00 МСК (05:00 UTC)
    - cron: "0 5 1 * *"
    # Таблица 2: каждый понедельник в 06:00 МСК (03:00 UTC)
    - cron: "0 3 * * 1"
 
jobs:
  # ── Заказы за месяц → таблица 1 ─────────────────────────────────
  orders-monthly:
    name: "Заказы за месяц"
    runs-on: ubuntu-latest
    timeout-minutes: 30
    if: |
      (github.event_name == 'schedule' && github.event.schedule == '0 5 1 * *') ||
      github.event.inputs.script == 'все' ||
      github.event.inputs.script == 'заказы_месяц'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install -r requirements.txt
      - name: Запуск
        env:
          WB_TOKEN:       ${{ secrets.WB_TOKEN }}
          SPREADSHEET_ID: ${{ secrets.SPREADSHEET_ID }}
          GOOGLE_JSON:    ${{ secrets.GOOGLE_JSON }}
        run: python main_заказы_за_месяц_gh.py
 
  # ── Реклама финансы → таблица 1 ──────────────────────────────────
  ads-finance:
    name: "Реклама финансы"
    runs-on: ubuntu-latest
    timeout-minutes: 30
    if: |
      (github.event_name == 'schedule' && github.event.schedule == '0 5 1 * *') ||
      github.event.inputs.script == 'все' ||
      github.event.inputs.script == 'реклама_фин'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install -r requirements.txt
      - name: Запуск
        env:
          WB_TOKEN:       ${{ secrets.WB_TOKEN }}
          SPREADSHEET_ID: ${{ secrets.SPREADSHEET_ID }}
          GOOGLE_JSON:    ${{ secrets.GOOGLE_JSON }}
        run: python main_реклама_фин_gh.py
 
  # ── Финансовые отчёты → таблица 1 ────────────────────────────────
  fin-reports:
    name: "Финансовые отчёты"
    runs-on: ubuntu-latest
    timeout-minutes: 30
    if: |
      (github.event_name == 'schedule' && github.event.schedule == '0 5 1 * *') ||
      github.event.inputs.script == 'все' ||
      github.event.inputs.script == 'фин_отчеты'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install -r requirements.txt
      - name: Запуск
        env:
          WB_TOKEN:       ${{ secrets.WB_TOKEN }}
          SPREADSHEET_ID: ${{ secrets.SPREADSHEET_ID }}
          GOOGLE_JSON:    ${{ secrets.GOOGLE_JSON }}
        run: python main_фин_отчеты_gh.py
 
  # ── Заказы за неделю → таблица 2 ─────────────────────────────────
  orders-weekly:
    name: "Заказы за неделю"
    runs-on: ubuntu-latest
    timeout-minutes: 30
    if: |
      (github.event_name == 'schedule' && github.event.schedule == '0 3 * * 1') ||
      github.event.inputs.script == 'все' ||
      github.event.inputs.script == 'заказы_неделя'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install -r requirements.txt
      - name: Запуск
        env:
          WB_TOKEN:         ${{ secrets.WB_TOKEN }}
          SPREADSHEET_ID_2: ${{ secrets.SPREADSHEET_ID_2 }}
          GOOGLE_JSON:      ${{ secrets.GOOGLE_JSON }}
        run: python main_заказы_доработка.py
 
  # ── Реклама → таблица 2 ───────────────────────────────────────────
  ads:
    name: "Реклама"
    runs-on: ubuntu-latest
    timeout-minutes: 30
    if: |
      (github.event_name == 'schedule' && github.event.schedule == '0 3 * * 1') ||
      github.event.inputs.script == 'все' ||
      github.event.inputs.script == 'реклама'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install -r requirements.txt
      - name: Запуск
        env:
          WB_TOKEN:         ${{ secrets.WB_TOKEN }}
          SPREADSHEET_ID_2: ${{ secrets.SPREADSHEET_ID_2 }}
          GOOGLE_JSON:      ${{ secrets.GOOGLE_JSON }}
        run: python main_реклама_доработка.py
 
